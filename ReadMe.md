# Simplest IPFS Video Player

> no npm && nodejs !!!

## Usage

* double click the `index.html`

## Note

* `IPFS` & `videostream` class is imported by `<script>` from [jsdeliver](https://cdn.jsdelivr.net).
* Be aware about the version.
